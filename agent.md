# Kat Chang site 工作藍圖

這份文件給後續協作代理使用，用來快速理解專案方向、內容標準、檔案分工與驗證步驟。開始改動前，先讀這份文件，再依實際任務查看相關頁面與資料檔。

## 專案定位

Kat Chang 網站是張雁雲營養師的專業展示、課程合作、衛教文章與互動教具入口。品牌核心是把高齡營養、疾病營養、精準營養、健康促進、藥膳、植物草藥與芳療等知識，整理成民眾聽得懂、課堂能操作、合作單位能採用的內容。

主要受眾：

- 中高齡族群與照顧者
- 企業健康促進與講座承辦單位
- 社區、長照、醫療與教育現場工作者
- 尋找營養諮詢、課程合作、教材設計的人
- 需要互動衛教工具的講師與活動帶領者

內容語氣要有專業感，也要保留親切感。句子要乾淨，少官腔，避免空泛形容。營養、醫學、保健食品、草藥與芳療相關內容，需要清楚標示適用情境、限制與參考來源。

## 工作原則

- 改動前先確認目標：這次是修版面、補內容、做 SEO、加文章、加教具，還是修互動功能。
- 優先維持現有靜態網站架構：HTML、CSS、原生 JavaScript、JSON 資料檔。
- 修改範圍要小而準，避免順手重構無關檔案。
- 使用繁體中文，面向台灣讀者。健康用語以台灣常見說法為主。
- 文章連載產物固定集中在 `work/2026-08-15-seo-review-docs/`，只使用其中既有的 `output`、`source`、`render` 三個分類位置。後續不要依日期、章節或單次執行新增文章資料夾，整理前先確認檔案用途與引用。
- 清理 `work` 前先列出檔案、Git 追蹤狀態與專案引用。確定無用途的資料夾或檔案才可送資源回收筒，用途未明的內容移到既有 `source` 或 `render` 並用 `archive-` 前綴與暫存清單標示，禁止猜測性永久刪除。
- `render` 僅存放 DOCX 轉出的 PDF、PNG、逐頁視覺檢查與對應 QA 證據。舊章節審閱 HTML、JSON、來源文字與發布 manifest 依用途留在 `source`，不因章節編號強行搬移。轉頁工具未成功執行時，不建立假 render 成品。
- 健康建議要以學術期刊、臨床指引、政府或專業機構資料為依據。
- 實證內容要分清楚：已有人體研究支持、仍屬初步研究、傳統使用經驗、個案經驗。
- 遇到疾病、藥物、懷孕、手術前後、慢性腎臟病、癌症治療、吞嚥困難等情境，要加上就醫與專業評估提醒。
- 不把輔助療法寫成可取代醫療處置。
- 不承諾療效，不使用誇大字眼。

## 內容標準

文章與頁面文案建議採這個結構：

- 讀者正在遇到的問題
- 關鍵營養或健康概念
- 可執行的生活做法
- 適用對象與需要留意的人
- 參考資料或延伸閱讀

寫作時請做到：

- 標題明確，讓讀者一眼知道能得到什麼。
- 每段只講一個重點。
- 建議要具體，例如份量、頻率、食物例子、替代選項。
- 需要引用研究時，優先用近年指引、統合分析、隨機對照試驗、大型世代研究。
- 中文健康資訊可參考衛福部國健署、食藥署、台灣營養學會、各醫學會指引。
- 國際資料可參考 WHO、CDC、NIH、ESPEN、ASPEN、ADA、AHA、WCRF、Cochrane、PubMed 收錄期刊。
- 若資料可能已更新，查證當下最新版本再寫。

避免：

- 把單一研究寫成定論。
- 把保健食品、草藥、精油寫成疾病治療。
- 用恐嚇式標題或過度焦慮的語氣。
- 堆疊形容詞，卻沒有具體做法。
- 只寫口號，沒有讀者可採取的下一步。

## 網站結構

根目錄主要檔案：

- `index.html`：首頁，含品牌介紹、服務、教具、文章與聯絡入口。
- `about.html`：個人簡介、學經歷、專長、證照與資格。
- `class.html`：授課主題、課程合作與教學案例。
- `styles.css`：全站主要樣式。
- `app.js`：首頁文章載入與共用互動邏輯。
- `llms.txt`：給搜尋與 AI 索引用的網站摘要。
- `robots.txt`、`sitemap.xml`：搜尋引擎爬取設定。

子目錄：

- `blog/`：文章列表、文章頁模板、文章資料。
- `blog/posts.json`：文章資料來源，首頁與文章列表會讀取這份資料。
- `blog/images/`：文章圖片。
- `teach/`：互動衛教工具入口。
- `teach/Stress-Food/`：壓力飲食互動教具。
- `teach/emotion-cards/`：草木心語情緒觀察卡。
- `teach/nutrition-battle/`：營養知識互動挑戰。
- `teach/nutritionranking/`：食品營養排行與查詢工具。
- `teach/nutritionranking/` 是 NutriRank 唯一正式來源；舊的 `D:\@antigravity\nutritionranking` 已刪除。資料更新使用正式資料夾內的 `update-data.ps1` 與 `tools/transform-tfda-data.mjs`，從政府資料開放平臺 Dataset 8543 下載官方 JSON，先驗證筆數、食品 ID、首／中／末項與欄位，再更新 `nutrition_data.js`、頁尾詮釋資料日期及快取版本參數。
- `https://teaching-3809d.web.app/`：文字雲互動工具，從 `teach/` 入口頁外連。
- 文字雲是獨立的 Firebase Hosting 工具，必須保留 Firebase 設定注入與 Firestore 即時回饋。維護 GitHub Pages 一般頁面時，不得移除或改寫文字雲專案的 Firebase 設定。
- `/admin/` 是本專案必須長期保留的 Blog 管理頁。頁面可新增、修改、刪除文章、上傳封面圖、設定首頁精選與預覽，使用者目前對這組功能與頁面表示滿意。除非使用者明確要求修改管理功能，任何網站維護都不得刪除 `admin/`、移除 Blog 的管理入口、改掉現有欄位或破壞既有發布流程。使用者需在當次頁面輸入有 `Contents: Read and write` 權限的 GitHub token。token 只存於瀏覽器記憶體，不寫入網站或 localStorage。
- 2026-07-27 曾誤刪管理頁、管理程式與 Blog 入口，造成文章只能閱讀。此事件已於 2026-08-13 依歷史版本恢復，列為維護禁忌。日後修改公開網站前，需先確認 `admin/index.html`、`admin/admin.js` 與 Blog 管理入口仍存在，並在收工時驗證管理頁可開啟。
- `assets/`：個人照、課程頁素材與共用圖片。

## 頁面維護重點

首頁：

- 第一屏要清楚呈現 Kat Chang 與營養師身份。
- 服務入口、課程入口、教具入口、文章入口、聯絡入口都要可快速點到。
- 首頁教具區固定放三張卡片：Stress Food、草木心語情緒覺察卡、NutriRank 食品營養排行榜。Nutrition Battle 與文字雲放在 `teach/` 入口頁。
- 首頁文章區最多顯示精選文章，資料來源以 `blog/posts.json` 為準。

簡介頁：

- 學歷、證照、經歷需保持正確。
- 新增資格或合作經驗時，語氣要穩健，避免過度包裝。
- 證照圖片或個人圖像要保留替代文字。

授課頁：

- 課程主題要對應實際可提供服務。
- 每個課程區塊最好含對象、可學到的內容、活動形式。
- 若加入疾病營養主題，要補上實證依據與適用限制。

文章頁：

- **新文章發布日期與圖片命名硬性規範**：新文章發布時，文章發布日期（`post.date`）、文章 ID（如 `YYYY-MM-DD-slug`）與封面圖片檔名（如 `blog/images/YYYY-MM-DD-slug.png`），一律以「**正式推上 GitHub 當日**」的實際日期為準，確保發布時間、ID 與圖片日期三者完全一致，嚴禁殘留舊草稿日期。
- 新文章先更新 `blog/posts.json`，再補圖片。
- 圖片檔名必須包含「與發布日相同的 YYYY-MM-DD 前綴」與英文主題 slug，避免空白與特殊符號。
- 文章摘要要能被首頁卡片與搜尋結果直接使用。
- 書籍連載文章目前統一使用分類「書籍連載與營養知識」，同一連載的日期文章要維持一致。分類數量增加前，先評估讀者搜尋意圖、導覽與篩選價值，避免每篇文章各自新增分類。
- Blog 管理頁必須提供文章分類欄位；新增與編輯文章都要寫入 `category`，編輯既有文章時不得因重建物件而遺失原分類。發布後要在 Blog 列表與文章頁 DOM 分別核對分類顯示。

互動教具：

- 優先確保手機可操作。
- 按鈕、卡片、分數、結果提示要有清楚狀態。
- 教具內容若含營養判斷，資料來源要寫清楚。
- 遊戲結果避免做醫療診斷或個人化處方。
- `teach/` 內的獨立工具也要檢查品牌辨識、返回首頁與 375px 行動版寬度。若使用者指定工具頁只保留設計者說明與返回首頁，就不要強制套用主站頁尾、官方 Line、Email 或完整共用導覽。若保留工具自己的 app 皮膚，要確認它仍能清楚被辨認為本站內容，且不能讓文字或導覽超出畫面。
- 互動工具的 Firebase 設定、資料庫規則與第三方服務傳遞內容要一起檢查。不能提供公開讀寫的規則範例，也不能把完整設定與房間連結送到不必要的第三方服務。Nutrition Battle 使用 Firebase Anonymous Auth 時，畫面與 README 要說明需啟用匿名登入，房間資料要帶建立者 UID，Rules 要限制建立者、玩家 UID 與搶答資料。
- QR Code 若包含房間設定，只能在瀏覽器本機生成，不能把含設定的完整房間網址交給 QR Code 第三方服務。若完整設定仍需放入加入網址才能維持現有掃碼流程，要搭配受限 Rules、匿名登入與 Firebase 專案限制。

## SEO 與索引

每次新增頁面或改重要內容，檢查：

- `<title>` 是否清楚含品牌與頁面主題。
- `<meta name="description">` 是否可讀且含核心關鍵字。
- canonical 是否指向正確公開網址。
- Open Graph 標題、描述、圖片是否合理。
- 結構化資料若有更新，JSON-LD 要保持合法。
- `sitemap.xml` 是否需要新增網址。
- `llms.txt` 是否需要同步補新頁面或新教具。
- 文章模板與工具頁要有可被爬蟲直接讀到的 canonical、Open Graph 與適合頁面類型的結構化資料，不能只靠載入後的 JavaScript 補欄位。
- 更新頁面後要同步檢查 `sitemap.xml` 的網址與日期，並確認 `llms.txt` 沒有漏掉公開工具。
- 文章單頁可以由 JavaScript 依文章 ID 更新 canonical、Open Graph 與 BlogPosting，但原始 HTML 必須保留通用 fallback 欄位。工具入口補 `ItemList`，個別教具依頁面性質使用 `SoftwareApplication` 或 `CollectionPage`。

關鍵字方向：

- Kat Chang
- 凱特營養師
- 張雁雲營養師
- 中高齡營養
- 高齡營養
- 疾病營養
- 精準營養
- 功能醫學
- 企業健康促進
- 營養講座
- 衛教教材
- 互動衛教工具

## 開發與驗證流程

常用檢查：

- 查看變更：`git diff`
- 查看狀態：`git status --short`
- 搜尋文字：`rg "關鍵字"`
- 檢查 JSON：用 Node 或瀏覽器確認 `blog/posts.json` 可解析。
- 本機預覽：可用簡單靜態伺服器開啟根目錄。

網站版面與聯絡入口固定規則：

- 每次修改公開頁面，包含 HTML、CSS、文字、連結與互動功能，都要同步檢查桌機網頁版與手機版，不能只修單一畫面。若畫面結構、字級、按鈕或連結有變動，兩種版面都要一起確認。
- 主要內容頁頁尾聯絡區固定使用「官方 Line」與「Email」兩個按鈕，連結分別為 `https://lin.ee/uGHIQcH` 與 `mailto:594katchang@gmail.com`。
- 各頁頂端「聯絡」固定連到 `https://zcal.co/katchang`，使用 `target="_blank"` 與 `rel="noopener"` 開啟預約頁。
- 行動版文字要讓中高齡讀者容易閱讀。主站首頁、簡介、授課、Blog 與 teach 入口，除 h1、h2 外的內文、h3、h4、卡片說明、按鈕與導覽文字最低為 `1.2rem`，行距可縮至約 `1.55`。獨立工具保留自己的品牌樣式，但仍要逐頁檢查閱讀清楚、換行與畫面邊界。
- teach 與 blog 入口頁要維持淺色背景、深色標題，並保留頁尾聯絡區。

修改後至少檢查：

- 主要頁面能開啟。
- 導覽連結正確。
- 圖片路徑正確。
- 手機寬度沒有文字重疊。
- 行動版寬度至少檢查 375px 與 390px：導覽列、hero 標題、一般按鈕與主要卡片都要有足夠對比與可點擊空間。
- 深色 hero 區的白底按鈕要明確指定深色文字，避免文字繼承成白色。
- `teach/index.html` 的工具入口使用淺色頁面底、清楚的標題顏色與單欄行動版卡片，避免共用首頁深色 hero 背景造成標題消失。
- 共用樣式有更新時，同步調整各頁的 CSS 版本參數，避免公開頁沿用舊快取。
- Console 沒有明顯錯誤。
- 文章卡片與教具入口仍能載入。
- 文章 JSON 或其他可編輯資料若會進入 `innerHTML`，要做文字跳脫、HTML 白名單或可信任的清理流程。Blog 正文只能通過明確允許的標籤、屬性與網址協定，禁止事件屬性、script、style、iframe 與不安全連結。
- GitHub Pages 原始碼無法直接設定 CSP、HSTS、X-Frame-Options、Permissions-Policy 等 Response Header。若工具日後加入私人資料或登入，必須在 CDN、反向代理或其他部署層補標頭與存取控制，不能用 `noindex` 代替權限。
- NutriRank 行動版要檢查頁首網格、網站導覽、搜尋區、卡片與表格的 375px 水平邊界，不能只看 h1 與 h2。
- 手機字級修改要逐頁檢查 `index.html`、`about.html`、`class.html`、`blog/`、`teach/`、NutriRank、Paper Radar、Stress Food、Nutrition Battle 與情緒卡，不能只依賴共用 `styles.css`。各獨立工具的 CSS 都要有自己的行動版文字覆寫。

若改到互動工具，再補測：

- 開始、重玩、返回、分享或下載等按鈕可用。
- 小螢幕點擊區域夠大。
- 資料檔載入失敗時有基本提示。
- 分數、結果與狀態不會因重複操作而錯亂。

## GitHub Pages 部署排查

若收到 GitHub Actions 部署失敗信，先看該次 run 的 job log。若 `Checkout`、`Setup Pages`、`Upload artifact` 都成功，只有 `Deploy to GitHub Pages` 出現 `Deployment failed, try again later.`，排查順序如下：

- 先確認公開網站是否仍可開啟，避免把既有線上頁面誤判成下線。
- 重跑失敗 job 一次，確認是否為 GitHub Pages 短暫服務端錯誤。
- 若重跑後同樣失敗，檢查 `.github/workflows/pages.yml` 使用的 actions 版本是否已落後。優先用官方 GitHub release 資訊確認最新版，再更新 workflow。
- 若 workflow 已是新版且仍失敗，從 GitHub 後台檢查 Settings > Pages 的 Source 是否為 GitHub Actions，也檢查 `github-pages` environment 是否有卡住的 deployment 或保護規則。
- 本機 `gh` 若尚未登入，可用已連線的 GitHub Actions 工具讀取 job log 與重跑 failed jobs。若需要改 Pages 設定，仍需使用者在 GitHub 網頁後台確認。

## 本機 GitHub CLI 與 Git HTTPS 認證

- 多專案 Git 推送前，先確認 `gh auth status --hostname github.com` 顯示有效登入，再執行 `gh auth setup-git --hostname github.com --force`，讓 HTTPS Git 使用同一組 CLI 認證。
- `git ls-remote` 只能證明讀取能力，推送能力要用 `git push --dry-run` 實測，dry-run 不能當成實際發布。
- Fine-grained token 若只選一個 repository，不能代表多專案推送能力。多專案工作優先使用 GitHub CLI 的瀏覽器授權，並讓 Git HTTPS 走 GitHub CLI 認證橋接。
- 測試與收尾時保留現有 `agent.md`、`project-worklog.md` 修改，不能因驗證自動 commit 或 push。

## 專案收尾 SOP

- 任務完成前，先確認本次輸出是否真的可交付。至少檢查頁面能開、重要連結正常、資料檔未壞、這次改到的區塊沒有明顯版面錯誤。
- 若這次整理出可重複使用的提示寫法、內容模版、SEO 欄位結構、互動工具修正流程、文章資料格式或驗證步驟，要回寫到對應 skill、工具腳本、模板檔或專案說明，不要只留在對話裡。
- 若這次學到的是 Kat Chang 網站專案限定規則，像是品牌說法、頁面欄位、JSON 結構、SEO 欄位、教具資料格式、醫療風險提醒或驗證步驟，要更新本檔 `agent.md`。
- 若這次學到的是跨專案也通用的長期規則，像是 Codex 與 Antigravity 一起遵守的收尾流程、全域技能同步方式、全域寫作限制或全域驗證標準，要更新全域 `AGENTS.md`。
- 每次專案完成後，都要補一筆根目錄 `project-worklog.md`。工作日誌至少記：日期、任務、主要輸出、做過的驗證、錯誤或風險、這次新增的規則、是否已更新 skill、是否已更新 `agent.md` 或全域 `AGENTS.md`。
- 工作日誌要把「已完成」「已修正錯誤」「尚未完成」「仍有風險」分開記錄。dry-run、讀取成功、權限檢查成功都不能寫成實際發布已完成。尚未處理的檔案、待使用者確認事項與曾經失敗的步驟要保留在醒目段落。
- 若本次沒有新規則可沉澱，也要在工作日誌寫清楚「本次無新增可沉澱規則」。
- 若使用者明講不要寫日誌、不要改 skill、或不要改 agent 規則，才可略過該項。沒有明講時，收尾視為預設必做。

## 近期工作路線

短期：

- 修正現有 HTML 內文若有亂碼或過期資訊。
- 統一首頁、簡介頁、授課頁的品牌說法。
- 檢查所有外部連結是否仍有效。
- 整理 `blog/posts.json` 欄位命名與圖片對應。
- 讓 `llms.txt` 與實際頁面保持一致。

中期：

- 建立文章新增範本，含標題、摘要、分類、圖片、引用來源。
- 為每個互動教具補一段教學使用說明。
- 補強高齡營養、疾病營養、精準營養相關文章。
- 增加課程合作頁面的案例與可洽詢形式。
- 改善無障礙標籤、鍵盤操作與圖片替代文字。

長期：

- 建立內容引用資料庫，記錄常用指引與期刊來源。
- 擴充教具資料檔，讓營養內容更容易維護。
- 規劃文章分類頁與主題頁，例如高齡營養、疾病營養、企業講座、互動教具。
- 追蹤搜尋流量與讀者常見問題，再回頭調整內容架構。

## 代理回覆規範

回覆使用繁體中文。需要專業說明時可分段與條列，語氣保持清楚、溫暖、好讀。若使用研究或指引，提供來源名稱、年份、結論重點。若沒有查證最新資料，請明講資料狀態。

送出前請自我檢查：

- 有沒有誇大療效。
- 有沒有把輔助療法寫成醫療替代。
- 有沒有漏掉高風險族群提醒。
- 有沒有提到來源卻沒有給可查證線索。
- 有沒有使用不自然的套版句。
- 有沒有使用專案指定避開的標點或詞句。

## 公開論文讀書小站

- 公開頁固定位於 `teach/paper-radar/`，只讀取同資料夾的 `data/papers-public.json`。
- 公開資料只允許論文公開欄位、合法全文連結、分類、tags 與已完成的中文成果。資料檔由私人 Sites 的 Worker 公開匯出結果產生，必須收集所有符合資格的 `completed` review 與 digest，不得用少量本機 note 檔案推算。公開頁不得連接私人 Sites API、D1、R2、上傳、排程或寫入功能。
- `review` 需標示摘要層級與全文限制。`digest` 需有完整全文核驗旗標才可進入公開資料。
- 修改公開頁後，檢查 JSON 可解析、頁面連結、搜尋與手機版版面。公開資料檔不得放入 owner、私人路徑、token、API key、PDF 識別資訊或個人閱讀狀態。
- 公開文章發布前，要對指定段落做逐句文字核對，留意中文缺字、主詞、標點與語意是否完整。遠端更新後，重新核對目標文章 ID、遠端檔案 SHA、公開 JSON、文章頁回應與首頁 `showOnHome` 清單。
- 公開文章回修原章節內容時，要同步回查該章 Word 審閱主檔與 `source` 證據，保持公開稿、Word 與本機發布來源的句子一致。
- `/admin/` 的文章修改會透過 GitHub Contents API 寫入公開 `main`，不會自動更新桌機專案或 Word 審閱檔。發現公開檔案時間較新時，先下載遠端 `blog/posts.json` 並核對遠端 commit、blob SHA、逐篇欄位與正文，再把已確認差異保存回本機 Word 與 `source` 紀錄。同步期間不得自行改變 `showOnHome`，也不得把本機修改推回公開端。
- 公開頁顯示的是已完成且符合公開資格的成果，不等同私人論文庫總筆數。頁面使用與私人版一致的中文與英文主題詞展開，搜尋標題、作者、期刊、DOI、摘要、分類、tags 與中文成果內容。
- 公開頁固定每頁 50 筆，底部顯示筆數摘要、頁碼、上一頁與下一頁。搜尋或篩選改變時回到第 1 頁，摘要、筆記與自我測驗卡使用折疊區塊，避免卡片過長。
- 公開頁的折疊標籤固定使用中文，包括「查看摘要」「全文評讀」「品質評讀」「自我測驗」。成果標題依類型顯示「全文評讀：論文標題」或「品質評讀：論文標題」。
- 公開頁左上角品牌區固定沿用主頁的人像 logo、品牌名稱與首頁連結。
- 公開網站的人像 logo 固定使用 repo 內的 `assets/profile/kat-avatar.jpg`，不得使用 Firebase Storage download token URL。
- 上述頭像規則只適用 GitHub Pages 一般頁面，不能套用到 `teach` 外連的 Firebase 文字雲。
- 公開頁卡片的論文標題與期刊分類固定優先顯示繁體中文，原始英文欄位仍保留在搜尋資料中，避免中文檢索失效。
- `/admin/` 是使用者指定保留的公開 `noindex` 管理頁，仍屬公開網址，沒有帳號登入或存取控制能力。管理時只在可信任裝置使用，token 只存在當次頁面記憶體，不要把 token 貼到公共電腦、截圖、工作日誌或對話中。除非使用者明確要求搬遷或刪除，不能以一般安全建議刪除這個管理頁。若日後改成正式帳號登入，需移到具伺服器端驗證的私人環境。

## 收束

這個專案的核心，是把可信的營養與健康知識，轉成讀者願意讀、學員能互動、合作單位能採用的網站內容。每次改動都要同時照顧三件事：專業正確、使用順手、品牌溫度。
## SEO 書籍連載

- 每日 SEO 草稿從 `D:\@Codex\書籍\2026-07-29-Nutrition-Concepts-Controversies-17e\output` 依序取用一章，文章來源可回查原文 PDF：`D:\@Codex\書籍\2026-07-29-Nutrition-Concepts-Controversies-17e\source\Nutrition Concepts & Controversies 營養學概念與爭論(2027).pdf`。
- 第一篇固定先介紹全書特色、強項、適合讀者與閱讀價值，預定 2026-08-13 執行。第二篇從 2026-08-14 開始寫 Chapter 1，之後每天依序推進一章至 Chapter 15。
- 書籍介紹篇要以原文與整理成果核對書籍資訊，不提前代寫章節內容。章節文章固定定位為「書籍的重點和心得整理」，先保留原章節主題、核心問題、概念順序與章末爭議，再轉成一般讀者能理解與執行的內容。
- 每日章節進度記錄放在專案私有工作區 `.codex\seo\book-series-progress.md`。只有實際完成待審草稿後才更新進度。
- 書籍連載仍遵守 SEO 文章標準：正文至少 2,000 字、具吸引力的開場、清楚搜尋意圖、表格與可執行步驟、作者專業判讀、可查驗來源、FAQ 與 AI 容易擷取的段落結構。
- 排程可以更新連載進度檔，但不得自行修改網站、發布文章、投稿、寄信或推送 GitHub。使用者確認品質後，才進入 blog 編排與發布流程。
- 每篇章節整理發文的開頭要明確標示書名、版本、Chapter 編號、英文章名與中文章名，並寫明本文是該章節的整理發文。
- 使用者確認標題與開場後，發布流程固定包含已確認用語修正、文章內相關連結、首頁曝光設定、BlogPosting、FAQPage、canonical、sitemap、llms.txt，以及桌機、行動版、公開頁面與 GitHub Pages 部署核對。
- 發布後要把當日狀態與下一章寫回 `.codex\seo\book-series-progress.md`，並在 `project-worklog.md` 分開記錄已完成、已修正錯誤、尚未完成與仍有風險。Search Console 數據需在可連線時另行查核，不能以推測代替。
- 後續 SEO 排程、開工、收工與網站命令固定以 `D:\@Codex\594katchang-source.github.io-main` 為專案根目錄。可延續的 SEO 工作摘要放在 `.codex\seo\context.md`，該資料夾由 `.gitignore` 排除，不得發布到 GitHub Pages。


## 文章資料並行寫入保護

- `blog/posts.json` 是共享文章資料。任何新增文章或 SEO 發布都要先讀取遠端 `main` 的最新版本與檔案 SHA，再只合併本次明確授權的目標文章。
- 不得用舊本地快照整份覆蓋遠端，也不得用 force push 掩蓋差異。寫入前要確認非目標文章的 ID、標題、內文、圖片、摘要、關鍵字與欄位值都未改變，出現未授權變動就停止。
- `showOnHome` 只由使用者在 Blog Admin 明確勾選或取消。SEO 新文預設為 `false`，不得因新增或更新文章自行增加首頁曝光。發布前要核對首頁精選 ID 與數量，最多四則。
- 使用 GitHub Contents API、blob、tree 與 commit 發布時，索引檔插入內容要使用實際換行字元。更新 ref 前，需把新增目標列移除後與遠端基準檔逐字比對，避免把 `\\r\\n` 或 `\\n` 文字誤寫進 XML、HTML。
- 若 GitHub 連線層因公開整檔包含既有聯絡方式、資格或其他未經本次核實的主張而拒絕 blob，該檔案要維持未變更，不得改用繞行方式重送。可先完成風險較低且明確授權的目標檔，並在工作日誌分開標示未更新檔案。

## SEO 連載用語與 Word 審閱檔

- 每日書籍連載文章的快速摘要固定使用「省時版本：」。依使用者審閱偏好，正文導讀採「文章性質：」後緊接兩段省時版本：第一段把章節原本的核心問題或開場直接收在「省時版本：」後，第二段合併實用判讀與閱讀地圖；不另設「省時版本」H2，也不把這一段做成問題判斷表，除非使用者另行要求。
- 文章正文、標題、摘要、FAQ 與 Word 審閱檔中的 `Controversy` 統一寫成「爭議」，章次統一寫成中文，例如第一章、第二章。
- FAQ 小標固定採用「FAQ：<章節主題>常見問題」格式，避免使用「常見問題與第六章的回答」這類句型。
- 營養師判讀段落標題固定使用「Kat Chang 營養師的判讀」，不要加入「與內容限制」等附加字樣。內容限制另放在判讀段落後或待確認事項。
- 判讀段落以章節重點歸納與直接的營養師心得為主，可使用「關於<主題>的五個重點」與編號條列，再接生活判讀。不要特別設計「我讀到這裡的第一個心得」或「我會把本章讀成」作為段落入口。
- 每個章節固定保留同一份完整 SEO 審閱來源，並產出 Word 審閱檔。內容需包含 SEO 欄位、正文、作者判讀、SEO 描述、分類標籤、slug、站內連結、FAQ、結構化資料、來源支持範圍、實際字數、原創差異化主張與待確認事項。
- Word 審閱檔與網站正文要由同一份來源產出。文件需完成 DOCX ZIP、段落、表格、標題、來源連結與內容一致性檢查。若環境缺少 LibreOffice，需明確記錄尚未完成轉頁與逐頁畫面檢查。
- Word 來源連結同步時，必須同時解析一般 `w:hyperlink` 與 Word `HYPERLINK` 欄位型連結；不能只保留顯示文字而遺失實際網址。發布 HTML／JSON、Markdown 與公開 DOM 都要核對重要來源連結可點擊。
- 若使用者明確要求送出 GitHub／公開發布，公開正文不得保留「待審稿」「待確認」等內部狀態標籤；發布前要先檢查，若 Word 審閱主檔仍有標籤，必須明確決定同步移除或停止發布，不能默默帶上線。
- 每次推送 GitHub 前，必須掃描公開 `blog/posts.json` 的所有文章欄位與正文，禁止出現「待審」「待審稿」「待人工審閱」等內部審閱字眼；命中時先移除公開狀態文字或停止推送，Word 審閱主檔可保留內部狀態。
- 站外來源稽核要分開統計「正文」與「SEO／研究來源」：正文中的每個來源標示都必須保留可點擊 URL；SEO／研究來源區的連結不會因存在於 Word 就自動發布，發布後要列出未進入正文的項目，不能誤報為正文斷線。
- 每日章節收尾閘門固定為：先確認 `output` 下同名 Markdown 與 Word 均存在，再核對 Word 章次、H1/H2/H3 順序、正文字數、來源連結、表格尺寸與禁用詞掃描，所有項目通過後才可更新 `book-series-progress.md`。
- 章節建置腳本必須明確接收來源 Markdown 與目標 DOCX 路徑，禁止沿用上一章的硬編碼輸出。建置前後都要檢查目標路徑，避免重寫既有審閱檔。
- Word 尚未建立或檢查中止時，章節狀態只能記為未完成，聊天內容與 Markdown 都不能取代 Word 審閱檔。
- Blog 文章列表固定依有效 `date` 由新到舊排序。列表搜尋需查標題、摘要、正文、關鍵字與分類，分類選單從文章資料動態產生，缺少分類的舊文章歸入「未分類」。此功能不得改動文章正文、圖片或 `showOnHome`。

## 書籍連載風格鎖定與產出流程

- Kat Chang 書籍連載的風格基準固定取 `work/2026-08-15-seo-review-docs/output/` 內第一至第五章 Word 成品。第六章 Word 已由使用者修改，只作內容核對與保護檔案之用，不作格式模板，Codex 不得自行改寫。
- Antigravity 比較稿或中間檔若已送進資源回收筒，不得表述為仍可直接讀取或持續學習的來源。若需說明其特徵，只能引用使用者當次貼出的文字、目前保留的檔案或已記錄且可追溯的觀察，並清楚區分來源狀態。
- 使用者說「刪除」或「不要更動」時，回覆前先核對檔案目前狀態與必要的 SHA-256。要分清楚目前檔案、資源回收筒檔案與永久刪除，不能用推測補足來源狀態。
- 產出新章節前，先讀第一至第五章的固定 H1 順序、正文導讀、`省時版本：`、判讀段落與 FAQ 標題，再開始寫作。第一、第二章的早期差異保留作歷史參考，第三至第五章的定型格式作為後續主基準。
- 固定文件骨架為：SEO 標題、搜尋字詞與意圖、文章摘要與開場、正文、SEO 描述、分類標籤與 slug、站內連結、FAQ 與結構化資料、文章結構化資料、來源支持範圍、字數與待確認事項。正文仍以書籍章節主線為核心。
- SEO 標題、正文主標與搜尋結果標題不加「第 N 章整理」或「書籍重點與心得整理」字樣。章次可留在正文書籍識別、摘要、來源與 metadata，文章標題聚焦讀者要解決的營養問題。使用者刪除標題尾端的「生活判讀」等字樣時，後續不得自行補回。
- 正文先交代書籍、章次、文章性質與「省時版本：」，再依原章節概念順序整理，接上台灣生活情境、營養師判讀、可執行步驟與適用限制。搜尋結構用來協助閱讀，不得取代書籍重點與心得整理。
- 正文中的「文章性質：」後面固定接兩段「省時版本：」短文；第一段不可再留一段獨立的同義開場，第二段要把生活判讀與章節閱讀路線說清楚。除非使用者另行要求，不在這個位置插入省時判斷表。礦物質分類固定使用「巨量礦物質」與「微量礦物質」；若出現在喝水段落中，應另設清楚的小標題與段落，避免把不同主題硬塞在同一段。
- 對於「水會跟著鹽走」「不要只靠白開水處理」等容易產生歧義的衛教語句，需改寫成說明水分、溶質、電解質與醫療評估之間關係的具體句子；安全限制要交代為什麼不能自行處理，不只保留警告語。
- `Kat Chang 營養師的判讀` 段落以章節重點、直接判讀、條列整理與生活應用為主。可用「關於主題的重點」搭配編號條列，數量依章節內容決定，不硬套固定數目。避免用第一人稱閱讀路線或另設「第一個心得」作為開頭。
- FAQ 小標沿用「FAQ：章節主題常見問題」。內容限制與就醫提醒維持在判讀後的獨立段落或既有安全提醒位置，不併入判讀標題。
- 產出後執行固定回查：掃描 `先給閱讀地圖`、`先給答案`、`常見問題與第六章的回答`、`與內容限制`、`我讀到這裡的第一個心得` 與 `我會把本章讀成`，命中時先停下修正。再核對 H1 順序、判讀標題、FAQ 格式、`Controversy` 是否均寫成「爭議」，以及第六章 Word SHA-256 是否維持原值。

## EAP 企業方案與商業合作定位

- 宇聯心理健康產業 / 宇聯 EAP 為既有合作夥伴，不列入冷開發名單，可作為向其他 EAP 方案公司（如鉅微、寬欣、旭立、華人心理）與科技廠福委會提案時的成熟合作實績背書。
- 企業健康促進與 EAP 提案聚焦於三大模組：① 科技業抗疲勞與高專注力飲食、② 健檢紅字脂肪肝/代謝症候群實證逆轉、③ 結合園藝治療（CHT）與芳療之身心減壓工作坊，以及員工 1-on-1 個別化諮詢。

## 【硬性規範】全站 SEO & GEO (AI 搜尋引擎) 自動化同步 SOP

未來任何協作代理或維護任務，凡涉及**新增文章、更新頁面、新增/調整教具或修改站內結構**時，必須**立即執行並落實以下 6 大 SEO & GEO 同步機制**，確保搜尋引擎（Google/Bing）與 AI 生成式搜尋（ChatGPT/Perplexity/Claude/Gemini）100% 同步檢索：

1. **一鍵同步工具呼叫**：
   - 每次內容更新後，必須執行 `python tools/sync_seo_and_geo.py`，全自動更新全站地圖與 AI 知識庫。
2. **Sitemap 雙軌維護**：
   - `sitemap.xml`：確保包含所有核心頁面、教具與文章，所有 `<lastmod>` 更新為當日日期，優先級（priority）與更新頻率（changefreq）設定正確。
   - `sitemap.html`：保持美觀之 HTML 網站導覽頁，並包含 `BreadcrumbList` 與 `CollectionPage` Schema.org 結構化資料。
3. **AI 搜尋引擎 (GEO) 知識庫深度同步**：
   - `llms.txt`：作為全站核心導覽與快速摘要。
   - `llms-full.txt`：作為深度知識庫，必須完整收錄全站每篇文章的深度臨床摘要、核心論點、關鍵字、E-E-A-T 作者背書與原始文章 URL。
4. **爬蟲權限 (robots.txt) 嚴格放行**：
   - 確保 `robots.txt` 宣告雙 Sitemap，並放行所有主流 AI Agent（`GPTBot`、`ChatGPT-User`、`OAI-SearchBot`、`ClaudeBot`、`Claude-Web`、`PerplexityBot`、`Google-Extended`、`Applebot-Extended`、`Amazonbot`、`cohere-ai`、`meta-externalagent`）。
5. **靜態爬蟲 Fallback (SEO Pre-rendered Links)**：
   - 確保 `blog/index.html` 內具備 `<noscript>` 靜態文章列表區塊，確保不執行 JavaScript 的輕量爬蟲能在 0 延遲下抓取全站文章。
6. **站內互鏈網絡 (Internal Cross-Linking Network)**：
   - 單篇文章底部必須具備「💡 延伸閱讀・精選衛教文章」推薦卡片（由 `blog.js` 的 `renderRelatedPosts` 動態關聯同類文章）。
   - 教具目錄頁（`teach/index.html`）必須保持反向導流至衛教專欄的推薦卡片。
   - 全站 6 大核心頁面 Footer 必須保留「網站地圖 (Sitemap)」文字錨點。

## 【使用者偏好與硬性排版規範】全站文字大小與封面圖卡防裁切

為保障中高齡讀者、手機與桌機使用者的閱讀舒適度，所有網頁開發與樣式維護必須嚴格遵守以下規則：

1. **全站最小文字大小規範（全站所有文字 >= 16px / 1rem）**：
   - 包含主網站所有頁面（首頁、簡介、授課、文章列表、單篇文章）以及所有獨立分頁與教具（如公開版論文讀書小站 `teach/paper-radar/`、各互動工具）：
   - 所有元素（正文 `p`、清單 `li`、作者/日期/分類 `post-meta`、關鍵字 Tag `keywords span`、搜尋框與下拉選單 `input/select/label`、搜尋結果提示、連結按鈕 `btn`、表格內文、頁尾版權等）**最小字體一律不得低於 16px (1rem)**。
   - 行動版（<=520px / <=760px）的段落與標題維持在 `1.05rem` - `1.25rem`，行高維持 `1.6` - `1.85`，嚴禁出現小於 16px 的微小字體。

2. **標題封面圖卡與列表縮圖防裁切規範（100% 完整呈現圖片文字）**：
   - **單篇文章封面圖卡 (`.article-cover`)**：
     - 一律採用 `object-fit: contain`、`width: 100%`、`height: auto`，並在桌機版設定合適的最大寬度（如 `max-width: 760px`）與置中排版，搭配柔和襯底（`#f0f5f2`）與陰影。
     - **嚴禁使用強制高度的 `object-fit: cover` 截斷圖片上下的標題文字、圖標或註解**。
   - **文章列表縮圖卡片 (`.post-thumb`, `.home-post-card img`)**：
     - 一律採用 `object-fit: contain` + `16:9` 比例容器與襯底，確保各長寬比圖卡（16:9、4:3、1:1 等）均能完整收納在框內，絕不切掉任何邊界文字。
     - 桌機版給予適中寬度（`240px` - `260px`），手機版（<=960px / <=520px）自適應螢幕寬度並限制最大高度（`200px` - `260px`），兼顧版面整潔與圖文清晰度。

3. **合作夥伴單位與過往授課主題維護規範**：
   - **合作夥伴新增**：依相同屬性分類（`01 政府與公部門`、`02 醫療與學校`、`03 企業與品牌`、`04 社區與親子場域`），新單位固定排序於該分類既有舊單位之後。
   - **授課題目歸整**：依課程主題本質放入最精準分類（預防飲食、職場主題、醫療飲食、咀嚼吞嚥、植物輔療、芳香調理、美容婦幼、瘦身主題、保健食品、營養素主題等），遇到重複主題則跳過，標點符號與項目條列格式保持統一。

4. **完整收工與發布流程**：
   - 每次完成網站修改，必須同時更新連動的靜態頁面、樣式表與快取版本參數（`?v=...`）。
- `Kat Chang 營養師的判讀` 段落以章節重點、直接判讀、條列整理與生活應用為主。可用「關於主題的重點」搭配編號條列，數量依章節內容決定，不硬套固定數目。避免用第一人稱閱讀路線或另設「第一個心得」作為開頭。
- FAQ 小標沿用「FAQ：章節主題常見問題」。內容限制與就醫提醒維持在判讀後的獨立段落或既有安全提醒位置，不併入判讀標題。
- 產出後執行固定回查：掃描 `先給閱讀地圖`、`先給答案`、`常見問題與第六章的回答`、`與內容限制`、`我讀到這裡的第一個心得` 與 `我會把本章讀成`，命中時先停下修正。再核對 H1 順序、判讀標題、FAQ 格式、`Controversy` 是否均寫成「爭議」，以及第六章 Word SHA-256 是否維持原值。

## EAP 企業方案與商業合作定位

- 宇聯心理健康產業 / 宇聯 EAP 為既有合作夥伴，不列入冷開發名單，可作為向其他 EAP 方案公司（如鉅微、寬欣、旭立、華人心理）與科技廠福委會提案時的成熟合作實績背書。
- 企業健康促進與 EAP 提案聚焦於三大模組：① 科技業抗疲勞與高專注力飲食、② 健檢紅字脂肪肝/代謝症候群實證逆轉、③ 結合園藝治療（CHT）與芳療之身心減壓工作坊，以及員工 1-on-1 個別化諮詢。

## 【硬性規範】全站 SEO & GEO (AI 搜尋引擎) 自動化同步 SOP

未來任何協作代理或維護任務，凡涉及**新增文章、更新頁面、新增/調整教具或修改站內結構**時，必須**立即執行並落實以下 6 大 SEO & GEO 同步機制**，確保搜尋引擎（Google/Bing）與 AI 生成式搜尋（ChatGPT/Perplexity/Claude/Gemini）100% 同步檢索：

1. **一鍵同步工具呼叫**：
   - 每次內容更新後，必須執行 `python tools/sync_seo_and_geo.py`，全自動更新全站地圖與 AI 知識庫。
2. **Sitemap 雙軌維護**：
   - `sitemap.xml`：確保包含所有核心頁面、教具與文章，所有 `<lastmod>` 更新為當日日期，優先級（priority）與更新頻率（changefreq）設定正確。
   - `sitemap.html`：保持美觀之 HTML 網站導覽頁，並包含 `BreadcrumbList` 與 `CollectionPage` Schema.org 結構化資料。
3. **AI 搜尋引擎 (GEO) 知識庫深度同步**：
   - `llms.txt`：作為全站核心導覽與快速摘要。
   - `llms-full.txt`：作為深度知識庫，必須完整收錄全站每篇文章的深度臨床摘要、核心論點、關鍵字、E-E-A-T 作者背書與原始文章 URL。
4. **爬蟲權限 (robots.txt) 嚴格放行**：
   - 確保 `robots.txt` 宣告雙 Sitemap，並放行所有主流 AI Agent（`GPTBot`、`ChatGPT-User`、`OAI-SearchBot`、`ClaudeBot`、`Claude-Web`、`PerplexityBot`、`Google-Extended`、`Applebot-Extended`、`Amazonbot`、`cohere-ai`、`meta-externalagent`）。
5. **靜態爬蟲 Fallback (SEO Pre-rendered Links)**：
   - 確保 `blog/index.html` 內具備 `<noscript>` 靜態文章列表區塊，確保不執行 JavaScript 的輕量爬蟲能在 0 延遲下抓取全站文章。
6. **站內互鏈網絡 (Internal Cross-Linking Network)**：
   - 單篇文章底部必須具備「💡 延伸閱讀・精選衛教文章」推薦卡片（由 `blog.js` 的 `renderRelatedPosts` 動態關聯同類文章）。
   - 教具目錄頁（`teach/index.html`）必須保持反向導流至衛教專欄的推薦卡片。
   - 全站 6 大核心頁面 Footer 必須保留「網站地圖 (Sitemap)」文字錨點。

## 【使用者偏好與硬性排版規範】全站文字大小與封面圖卡防裁切

為保障中高齡讀者、手機與桌機使用者的閱讀舒適度，所有網頁開發與樣式維護必須嚴格遵守以下規則：

1. **全站最小文字大小規範（全站所有文字 >= 16px / 1rem）**：
   - 包含主網站所有頁面（首頁、簡介、授課、文章列表、單篇文章）以及所有獨立分頁與教具（如公開版論文讀書小站 `teach/paper-radar/`、各互動工具）：
   - 所有元素（正文 `p`、清單 `li`、作者/日期/分類 `post-meta`、關鍵字 Tag `keywords span`、搜尋框與下拉選單 `input/select/label`、搜尋結果提示、連結按鈕 `btn`、表格內文、頁尾版權等）**最小字體一律不得低於 16px (1rem)**。
   - 行動版（<=520px / <=760px）的段落與標題維持在 `1.05rem` - `1.25rem`，行高維持 `1.6` - `1.85`，嚴禁出現小於 16px 的微小字體。

2. **標題封面圖卡與列表縮圖防裁切規範（100% 完整呈現圖片文字）**：
   - **單篇文章封面圖卡 (`.article-cover`)**：
     - 一律採用 `object-fit: contain`、`width: 100%`、`height: auto`，並在桌機版設定合適的最大寬度（如 `max-width: 760px`）與置中排版，搭配柔和襯底（`#f0f5f2`）與陰影。
     - **嚴禁使用強制高度的 `object-fit: cover` 截斷圖片上下的標題文字、圖標或註解**。
   - **文章列表縮圖卡片 (`.post-thumb`, `.home-post-card img`)**：
     - 一律採用 `object-fit: contain` + `16:9` 比例容器與襯底，確保各長寬比圖卡（16:9、4:3、1:1 等）均能完整收納在框內，絕不切掉任何邊界文字。
     - 桌機版給予適中寬度（`240px` - `260px`），手機版（<=960px / <=520px）自適應螢幕寬度並限制最大高度（`200px` - `260px`），兼顧版面整潔與圖文清晰度。

3. **合作夥伴單位與過往授課主題維護規範**：
   - **合作夥伴新增**：依相同屬性分類（`01 政府與公部門`、`02 醫療與學校`、`03 企業與品牌`、`04 社區與親子場域`），新單位固定排序於該分類既有舊單位之後。
   - **授課題目歸整**：依課程主題本質放入最精準分類（預防飲食、職場主題、醫療飲食、咀嚼吞嚥、植物輔療、芳香調理、美容婦幼、瘦身主題、保健食品、營養素主題等），遇到重複主題則跳過，標點符號與項目條列格式保持統一。

4. **完整收工與發布流程**：
   - 每次完成網站修改，必須同時更新連動的靜態頁面、樣式表與快取版本參數（`?v=...`）。
   - 當使用者同意發布時，必須執行 Git Commit & Push 至遠端儲存庫，確保 GitHub Pages 線上生效，不得將樣式或腳本遺漏在本機。
   - 每次任務結束前，必須將最新規則沉澱至 `agent.md`，並在 `project-worklog.md` 留下完整工作紀錄，方完成收工。

## 書籍連載 Word 交付規則

- 每章待審文章固定交付 Word 審閱檔。Markdown 只作為來源或轉換中間檔，不能以 Markdown 取代 Word 審閱檔。
- Word 與同源 Markdown 放在 `work\2026-08-15-seo-review-docs\output`，研究回報與建檔腳本放在 `source`，轉頁與檢查資料放在 `render`。沿用既有三個位置，不依日期新增文章資料夾。

## 企業健康講座與全站核心關鍵字寫作嵌入規範

未來在進行書籍連載專文（Chapter 9～15）、衛教文章、互動教具說明、下載手冊或著陸頁寫作時，必須依主題情境優先融入以下核心關鍵字體系，兼顧大眾搜尋意圖與 AI 搜尋引用：

### 1. 企業採購、HR / EAP 與講師合作核心關鍵字（使用者指定）
- **企業採購大詞**：`企業健康講座`、`EAP 員工健康講座`、`職場健康促進講座`、`企業營養講座推薦`、`公司健康日講座規劃`、`福委會健康活動推薦`
- **接案身份與地域**：`健康講座接案講師`、`企業外聘營養講師`、`台北營養師演講邀約`、`桃園營養師演講邀約`
- **特色體驗手作**：`辦公室輕食手作示範`、`植物芳香手作營養工作坊`（嚴格遵守現場 100% 零明火安全）
- **白領痛點主題**：`上班族外食抗疲勞飲食`、`高階主管減壓與護心飲食`、`體重管理與外食減醣工作坊`、`預防脂肪肝與三高飲食講座`、`辦公室微運動與飲食搭配`
- **樂齡與長照機構**：`樂齡大學健康講座`、`長照機構吞嚥防嗆培訓`、`高齡肌少症飲食講座`、`失智症預防飲食工作坊`

### 2. 三大商業支柱與專業品牌核心詞（歷史既有矩陣）
- **專業營養師**：`中高齡營養師`、`中高齡營養專家`、`台北營養師推薦`、`桃園營養師推薦`、`長照營養師`、`功能醫學門診`、`精準營養`
- **疾病與生活衛教痛點**：`肌少症飲食`、`蛋白質克數計算與吸收`、`穩定血糖早餐`、`全穀膳食纖維與添加糖`、`Omega-3 飽和脂肪抗發炎`、`水分平衡與電解質生活判讀`、`維生素 D 骨骼鈣化`、`食品營養標示法規`
